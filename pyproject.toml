[project]
name = "poc-tiny-tool-calling"
version = "0.1.0"
description = "Evaluate tiny language models on tool-calling ability."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = [
    "matplotlib>=3.11.1",
    "numpy>=2.4.6",
    "pandas>=3.0.5",
]
